#include "main.h"

/**
 * add - sums up two numbers
 * @a: first declared variable
 * @b: second declared variable
 * Return:Always (sum)
*/

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
